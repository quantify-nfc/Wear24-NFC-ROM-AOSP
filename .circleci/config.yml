version: 2
jobs:
  build:
    machine:
      image: circleci/classic:latest

    steps:
      - checkout
      - run:
          name: AOSP Build
          command: |
            git config user.email "test@example.com"
            git config user.name "CircleCI"
            ./repo-setup.sh -n
            ./build.sh -n

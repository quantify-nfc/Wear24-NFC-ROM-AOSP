version: 2
jobs:
  build:
    machine:
      image: circleci/classic:latest

    steps:
      - checkout
      - run:
          name: Setup
          command: |
            ./repo-setup.sh -n
      - run:
          name: AOSP Build
          command: ./build.sh
